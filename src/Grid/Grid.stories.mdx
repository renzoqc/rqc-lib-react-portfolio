import { Meta, Story, ArgsTable } from '@storybook/addon-docs';
import Grid from "./Grid";

<Meta title="Grid" component={Grid} />

# Grid

## Props
<ArgsTable of={Grid} />

## Description

`Grid` allows to use the grid layout more easily

## Demo

export const Template = (args) => <Grid {...args} />

<Story name="example" args={{row: true}}>
    {Template.bind({})}
</Story>
