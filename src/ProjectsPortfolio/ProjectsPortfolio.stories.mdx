
import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs';
import ProjectsPortfolio from './ProjectsPortfolio';

<Meta title="ProjectsPortfolio" component={ProjectsPortfolio} />

# ProjectsPortfolio

## ProjectsPortfolio props
<ArgsTable of={ProjectsPortfolio} />

## Description

`ProjectsPortfolio` component is the header of portfolio.

## Demo

export const Template = (args) => {
    return <ProjectsPortfolio {...args} />
}

<Story name="ProjectsPortfolio" args={{
    pathImage: 'img/charmander.png',
    altImage: 'portfolio',
    withText: true,
}}>
{Template.bind({})}
</Story>

